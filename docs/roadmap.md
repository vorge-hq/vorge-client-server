# Production Roadmap — Living Checklist

> **Current phase: P2 — Tenant isolation ✅ COMPLETE 2026-07-03; P3 write/section API next (paused for user go-ahead). P0 infra grounding COMPLETE 2026-07-03** (Supabase + Render `vorge-api-staging` + Vercel `vorge-app`, cross-site smoke passed). Locked decisions: Supabase = managed Postgres only (keep custom JWT/bcrypt/MFA auth); server = Render web service from `server/Dockerfile` (single instance — in-memory rate-limit/MFA caches); real client = NEW Vercel project (`VITE_ENABLE_DEMO=false`), demo project `vorge-demo-roles` untouched; AI = Vercel AI Gateway + AI SDK behind an app-layer module (§9 deviation SIGNED OFF 2026-07-03; write/section API contract extension SIGNED OFF 2026-07-03 — see `docs/decisions/2026-07-03-*`). Do not push past a phase without user review.

**How this file relates to the others:** `docs/production-status.md` is the product-state map; `docs/strategic-roadmap.md` is the evidence-backed retrospective/synthesis (audited 2026-06-04); this file is the production-push execution checklist. Tick boxes here (and append `SESSION_LOG.md`) on every meaningful change and before any commit touching `client/src/`, `server/src/`, or `server/migrations/`. Phase mapping to the old AGENTS.md numbering: old Phase 1 = P1 (done), old Phase 2 = P2, old Phase 3 ≈ P0 + P5.

**Standing rules for every phase:** `make test` before every commit; 95% server-service coverage gate stays; never commit `.env` (check `git status`); fresh prod secrets via `openssl rand -base64 32`; no sign-ups/deploys/spend by the agent — dashboard steps are handed to the user (see `docs/infrastructure.md`); `docs/businesslogic.md` and `docs/api-contract.md` are not edited — deviations go in `docs/decisions/`.

**Definition of done — tests are the gate:** every phase has a binding acceptance-test spec in **`docs/test-specs.md`**. A checklist item below may only be ticked when its spec'd tests exist, run inside `make test`, and pass. Read the spec's ground rules before writing any phase code.

---

## P0 — Infra grounding ✅ COMPLETE (2026-07-03)

Outcome: a real staging environment (Supabase DB + Render API + non-demo Vercel client), not fixtures.

Code/config changes (agent) — **landed 2026-07-03** (201 server / 144 client tests green):
- [x] Knex SSL + connection split: `knexfile.js` (migrations) prefers `MIGRATE_DATABASE_URL`, SSL in production or with `DATABASE_SSL=true`; app knex (`src/db/knex.js`) SSL via `DATABASE_SSL` (default on in production).
- [x] Migration `202607030001_enable_pgvector.js` (`CREATE EXTENSION IF NOT EXISTS vector`, warn-and-continue where binary absent); docker-compose db image → `pgvector/pgvector:pg16` (recreate local db container to pick it up).
- [x] Cookie cross-site fix: `COOKIE_SAME_SITE` env (default `strict`; boot guard rejects invalid values and `none` without Secure) now drives refresh + MFA-trust cookies (4 sites). Chunk-4 "SameSite locked to Strict" superseded — noted in the service docblock + SESSION_LOG. +9 env-guard tests.
- [x] `CORS_ORIGIN` verified: single exact origin supported and sufficient (prod domain only; preview URLs deferred).
- [x] Env examples: `.env.example` diff handed to user for human review (AGENTS.md rule — file not edited by agent).
- [x] Client prod env shape documented in `docs/infrastructure.md` §3/§6.

Manual dashboard steps (user — click-by-click in `docs/infrastructure.md`) — **DONE 2026-07-03**:
- [x] Supabase project `vorge-staging`; migrate (session pooler :5432) + seed; `vector` enabled; 22 tables.
- [x] Render web service `vorge-api-staging.onrender.com` (Docker, single instance, `/health` 200, `COOKIE_SAME_SITE=none`, DATABASE_URL = transaction pooler :6543).
- [x] Vercel client `vorge-app.vercel.app` (`VITE_ENABLE_DEMO=false`, `VITE_API_BASE_URL` → Render).
- [x] Smoke PASSED: login 200 + CORS(allow-origin+credentials) + refresh cookie `HttpOnly; Secure; SameSite=None` + `GET /api/assessments` returns 3 seeded rows (Render→Supabase pooler) + cookie-refresh issues a new token. Optional remaining: MFA enroll/verify browser eyeball (non-blocking; API plumbing verified).

**P0 COMPLETE 2026-07-03.** Interim posture: staging on Vercel URL (custom domains / vorge.io per-client portal routing deferred — see memory `vorge-io-portal-routing`).

## P1 — Auth ✅ done (record only)

Login → JWT → refresh (rotating httpOnly cookie + family revocation), session revocation/logout, password reset (email delivery stubbed — wired in P5), TOTP MFA + enforcement + trusted devices, demo personas env-gated. Chunks 0–4; records in `SESSION_LOG.md`, git tags, `docs/decisions/chunk-4-mfa.md`.

## P2 — Tenant isolation (P0 security — must land before real customer data) ✅ COMPLETE (2026-07-03)

DoD: `docs/test-specs.md` §P2 — integration harness (real Postgres, fail-loud without `TEST_DATABASE_URL`), route-guard introspection test, cross-tenant matrix, repo SQL-scoping assertions, RLS tests as the non-owner app role.

- [x] Build the integration-test harness + two-operator fixture (test-specs §P2 deliverable 0) — **DONE 2026-07-03**: `jest.integration.config.js` + `tests/integration/*`, fail-loud on missing `TEST_DATABASE_URL`, `npm run test:integration`. First test `tenantIsolation.repo.test.js` (9 cases) proves repo-layer isolation against real Postgres (Author/HQ/Admin scoping; cross-tenant get→null; scoped bundle). Local test DB: `vorge_test` on the docker pgvector db.
- [x] Route-guard introspection test (test-specs §P2 deliverable 1) — **DONE 2026-07-03**: `server/tests/middlewareCoverage.test.js` (18 cases, unit loop — no DB, so it gates every commit) walks `app._router.stack` and asserts every data route has `authenticate` + (`requireFacilityAccess` OR a documented in-test repo-scoped-getter allowlist entry). Structural guards: unclassified `/api` mounts fail, stale allowlist entries fail. `requireFacilityAccess` returns a named fn (`requireFacilityAccessMiddleware`) for detectability. Repo-scoped-getter equivalence recorded in `docs/decisions/2026-07-03-repo-scoped-facility-access.md`; AGENTS.md invariant 1 reworded to accept both guards. Red-checked (dropped-exemption, stale-entry, unclassified-mount all fail as expected).
- [x] Isolation guard on every data route (2026-07-03): resolved by the two-guard decision (`docs/decisions/2026-07-03-repo-scoped-facility-access.md`) — by-id routes enforce facility at the repo layer (user-scoped getter → 404), which is the `requireFacilityAccess` equivalent for routes with no facility in the payload; `requireFacilityAccess` middleware stays the required shape for P3 write endpoints whose payloads carry `facilityId`. Enforced by the deliverable-1 introspection test above.
- [x] `listAssessmentsForUser` scoping pushed into SQL (2026-07-03): `facilityScopeFor` builds facility/operator WHERE from the acting role's assignments; default-deny (empty → `[]`). Identical results to the old JS filter (integration test still green). Other repo queries still to audit.
- [x] Fixed the `mitigations` route `hasFacilityAccess: true` hardcode (2026-07-03) — now derived from `canAccessFacility` on the loaded mitigation.
- [x] RLS policies on all facility-scoped tables + non-owner-role proof (test-specs §P2 deliverable 4, **DB layer**) — **DONE 2026-07-03**: `migrations/202607030002_rls_policies.js` adds a uniform `facility_id = ANY(current_setting('app.current_facility_ids'))` policy (USING + WITH CHECK; unset → default-deny) to all 10 data tables, enabling RLS on the 3 that lacked it. NO `FORCE` (owner must bypass for seed/migrations). `tests/integration/rls.test.js` (7 cases) proves it **as a non-owner role** (`vorge_app_rls`, provisioned in `global-setup`): context→only-that-facility, no-context→0 rows, cross-tenant UPDATE→0 rows (owner-verified unchanged), cross-tenant INSERT→RLS-rejected, pooled sequential different-context txns no leak. Red-checked (permissive policy → 6/7 fail). **Inert until role switch:** app still connects as owner on staging, so zero behavior change until DATABASE_URL points at a non-owner role.
  - [x] **App wiring** — **DONE 2026-07-03**: `src/db/requestScope.js` (`AsyncLocalStorage` `activeConn()` + `runInFacilityScope`) + `src/middleware/facilityScope.js` (resolves the acting role to a concrete facility-id list — operator-wide roles expanded via a `facilities` lookup — and pins the request's DB work to it inside a txn running `set_config('app.current_facility_ids', …, true)`). Repos default `trx = activeConn()`, so reads/mutations auto-use the scoped txn; content mutations open an inner savepoint via `activeConn().transaction`. `facilityScopeFor` moved to `facilityAccessService`. `rlsWiring.test.js` proves it as the non-owner role (scoped read returns rows; no-context read returns `[]`; operator expansion; rollback). Hotfix 2026-07-03 (found by staging smoke): auth audit writes hit `audit_log_entries` RLS with no facility context → login 500; `appendAuditLog` now self-scopes to the entry's facility inside a txn/savepoint (also fixes a silent hash-chain break); `rlsWiring.test.js` +1 regression, red-checked.
  - [x] **User checkpoint (Supabase dashboard)** — **DONE 2026-07-03**: migration `202607030002_rls_policies` run on Supabase staging; non-owner role `vorge_app` + grants created in the SQL editor; Render `vorge-api-staging` `DATABASE_URL` repointed at `vorge_app` (transaction pooler); smoke passed (health + browser login/data on `vorge-app.vercel.app`). **RLS is now live/enforced on staging.**
- [x] Cross-tenant integration tests (2026-07-03): route matrix `tests/integration/tenantIsolation.test.js` (17 cases, supertest + real DB via the P2 harness). Op-A user targeting Op-B resource → GET :id 404 (ASSESSMENT_NOT_FOUND, no existence leak), list returns own-tenant rows only, workflow/mitigation-log mutations 404 **with the target row asserted unchanged in the DB**; wrong-role 403 (ROLE_NOT_ALLOWED + ROLE_NOT_ASSIGNED), unauthenticated 401. Covers every data route that exists today (assessments GET/list/workflow, mitigations mine/log, admin config); **mutation matrix extends when P3 write endpoints land** (test-specs §P2 note). Auth helper `tests/integration/session.js` mints a real session + bearer token mirroring the login route. Red-check verified (flipped a cross-tenant assertion to 200 → failed → reverted).
- [x] Operator-portfolio scoping for HQ Executive (§17.5) covered by tests (2026-07-03): route matrix asserts HQ Exec of Op-A sees ALL Op-A facilities and ZERO Op-B rows (+ Op-B mirror for symmetry).
- [x] Fix mitigations route hardcoding `hasFacilityAccess: true` (2026-06-04 audit finding, `docs/strategic-roadmap.md`) — **DONE 2026-07-03** (duplicate of the ticked item above): now derived from `canAccessFacility` on the loaded mitigation; covered by `tenantIsolation.test.js`.
- [x] Reconcile AGENTS.md invariant 2 wording (2026-07-03): code is correct (`isDemoEnabled()` gates on `VITE_ENABLE_DEMO === "true"`, deliberately not `import.meta.env.DEV`); aligned the doc to the code (heading → "flag-gated", mechanism corrected).

## P3 — Write/section API (P0-priority gap — the missing core)

There are NO write endpoints today (assessments API = GET /, GET /:id, POST /:id/workflow). All demo editing is client-side fixtures; a real Author cannot save anything. Contract extension approved 2026-07-03 (`docs/decisions/2026-07-03-write-section-api.md`; api-contract.md itself edited only on explicit instruction when P3 lands).

**Server-side complete (2026-07-03):** slices (a)–(f) landed & green (250 unit / 107 integration). Foundation: section-text migration (`assessment_sections`, decision recorded) + shared write-guard (`contentWriteGuard.runContentMutation`). Endpoints: Assets/Threats CRUD, links (PUT enable/disable), evaluations (PATCH), contributors (PUT), section text (PUT /sections/:n), withdraw/recall (lockVersion on /workflow), Lead Author reassignment (PUT /lead-author), mitigation owner assignment. Each proven with the §P3 suite (six-case, true lock_version race, state×role matrices, atomic audit, cross-tenant). Also fixed a read-your-writes gap in P2's `facilityScope` (commit-before-flush — `docs/decisions/2026-07-03-facility-scope-commit-before-flush.md`). **Remaining: (g) client flip** (prod mode off fixtures onto live calls + 409-reload RTL) — the only unticked P3 work.

- [x] CRUD endpoints: assets, threats, asset-threat links, evaluations, contributors (+ section-1/2/8 text — `assessment_sections` migration `202607030003`). **DONE 2026-07-03.**
- [x] Optimistic concurrency via existing `lock_version` on every mutation (409 on stale; true concurrent-race test). **DONE 2026-07-03.**
- [x] State/role guards: writes only by Author on Draft (per state machine); by-id routes repo-scoped (getter → 404), enforced by the introspection test. **DONE 2026-07-03.**
- [x] Audit entry on every mutation (`appendAuditLog`, atomic with the write; lowercase-hyphen vocabulary). **DONE 2026-07-03.**
- [x] Flip client prod mode off fixtures onto live calls. **Landed 2026-07-03:** (1) write seam — `client/src/api/assessmentApi.js` (wrappers for every P3 endpoint) + `WorkspaceContext.saveSectionText` prod↔demo branch; narrative saves for §1 Executive Summary + §8 Conclusion wired end-to-end (prod live PUT w/ lockVersion, 409 → reload affordance; demo no-fetch). (2) reads — `client/src/api/adapters.js` (server→client shape, defaults for demo-only fields); assessments **list** hydrates from `GET /api/assessments` in prod (server-scoped, no client role-narrowing — decision 2026-07-03); **workspace** hydrates the opened assessment (fields + section texts) from `GET /:id`. Demo keeps fixtures throughout (fetch-spy tests). **Landed 2026-07-04 — content-entity READS:** `hydrateAssessmentBundle` now also hydrates child entities (assets/threats/asset×threat links→matrix/evaluations) from the bundle in prod via new adapters (`toClientAsset/toClientThreat/toClientEvaluation/toClientLinks`); canonical client↔server JSONB field mapping fixed once in `docs/decisions/2026-07-04-content-entity-field-mapping.md` (columns + `details`/`r1`/`r2` bags; disabled links omitted; scores derived). Fetch-spy test extended (prod maps rows, demo no-op). **Landed 2026-07-04 — §2 Facility Info + Assets WRITES:** (1) §2 decided (serialize structured form → JSON in section-2 `content_text` via `PUT /sections/2`; no new server model) — `serializeFacilityInfo`/`parseFacilityInfo` + `FacilityInfoSection` init-from-JSON + save-on-blur (prod live PUT w/ lockVersion + 409 banner; demo no-fetch). (2) Assets (Section 3) write flip — `toServerAssetPayload` + prod-aware `addAsset`(POST)/`removeAsset`(DELETE)/`persistAsset`(PUT on blur) in `WorkspaceContext` (stateRef for fresh lockVersion, server-id reconciliation, 409 reload banner); demo unchanged. Both proven with fetch-spy tests. **Landed 2026-07-04 (later) — ALL remaining content-entity WRITES:** threats (`toServerThreatPayload` + add/persist/remove), asset×threat-link toggle (`toggleMatrix`→`PUT /links` enable/disable), evaluations (`toServerEvaluationPayload` + `persistEvaluation`→PATCH on blur), contributors (`saveContributors`→whole-list PUT). Each: local-optimistic per keystroke + blur/discrete persist through the prod↔demo seam, lockVersion synced from responses, 409 → reload banner; demo fires nothing. Fetch-spy suites for each section (threats/matrix/evaluations/appendices). **Server eval-create gap CLOSED 2026-07-04:** `setAssetThreatLink` now AUTO-CREATES an empty evaluation when a pair is enabled (none-exists), echoes it in the `PUT /links` response (`{ link, evaluation, lockVersion }`), and records `evaluationCreated` on the link audit; the client adds the returned (UUID) evaluation to local state so Section 6 can PATCH it immediately. +2 integration tests (auto-create on fresh enable; re-enable echoes existing without duplicating). **P3 (g) client flip COMPLETE — 250 unit / 176 client / 109 integration green.**
- [x] Withdraw/recall + Lead Author reassignment endpoints (§5.5–5.6) — `lockVersion` on `/workflow` closes the recall-race concern; `PUT /lead-author` (Lead Author or Admin, non-Approved, target-must-be-Author). **DONE 2026-07-03.**
- [x] Mitigation-assignment endpoints (§7 owner management) — `PUT /mitigations/:id/owner` through the write-guard. **DONE 2026-07-03.**
- [x] DoD: `docs/test-specs.md` §P3 — server portions DONE (six-case, true-race lock_version, state×role matrices, atomic audit, section-text round-trip + migration idempotency, cross-tenant matrix extended to every mutation). **Client flip DoD** (prod section-save fires live call w/ lockVersion; 409 renders exact reload copy; demo fires no fetch — fetch spy) DONE via `ExecutiveSummarySection.test.jsx`. **DONE 2026-07-03** (broad per-section rewire tracked above as remaining follow-on, not a §P3 DoD gap).

## P3.5 — Word/PDF export (pulled forward from P5 — approved 2026-07-03)

- [x] **Client Export button DONE 2026-07-04** — "Export document" in the workspace Tools rail opens `ExportModal` (Word/PDF chooser, non-final watermark note). Prod downloads via `apiDownload`→`GET /export?format=` (blob + Content-Disposition filename) and streams to the browser (`triggerBrowserDownload`); `WorkspaceContext.exportDocument` is the prod↔demo seam (demo fires no fetch, shows "available in the live app"). RTL fetch-spy suite `modals/ExportModal.test.jsx` (prod docx/pdf calls, watermark note by state, 403 inline error, demo no-fetch). 184 client tests green.
- [x] Standard SRA template export, Word + PDF (§16). **Server-side DONE 2026-07-04 (252 unit / 124 integration green).** `GET /api/assessments/:id/export?format=docx|pdf` (assessments router; repo-scoped-getter guard → allowlisted in `middlewareCoverage`). Word via `docx` npm; PDF via **`pdfkit`** (pure-JS, no headless browser — DoD PDF checks are minimal, so pdfkit meets them and stays fast/CI-deterministic; supersedes the roadmap's "print-CSS" suggestion). `exportService.js` renders both formats from one `deriveTables` source (cover, Document Approvals + Version Control front-matter, Sections 1–8 + Appendices, watermark). `exportRepository.js` assembles front-matter: approvals from lead-author + audit sign-off events (`review_completed`/`approved`), Version Control from `versions`, frozen snapshot when Approved. Export role = all section-readers (Author/Reviewer/Approver/HQ Exec/Admin); Mitigation Owner → 403. Watermark on any non-Approved state; every export writes an `export` audit row. **Client Export button = follow-on (no §P3.5 DoD spec'd for it).**
- [x] DoD: `docs/test-specs.md` §P3.5 — **DONE 2026-07-04** via `tests/integration/export.test.js` (15 cases): golden-content docx (9 headings ordered, approvals+contributors names, every mitigation row, name/facility — parsed with `mammoth`, not byte-compared), PDF smoke (`%PDF` magic + `pdf-parse` pages>0 + text has name), §16 role matrix (200 for readers, 403 Mitigation Owner, 404 cross-tenant, 401 unauth, 400 bad format), export audit row (one row, trace echoed; none on 403), Approved frozen-snapshot (renders snapshot not live edits, clean/no watermark), <30s perf guard.

## P4 — AI module + features (ROI order)

Foundation first: app-layer AI service module wrapping Vercel AI Gateway via the AI SDK — owns per-facility cost ceilings (soft 80% alert / hard suspend), audit-log writes (all §9.7 fields), per-facility prompt scoping, retries, rate limits. Feature code never imports a provider directly (§9's architectural rule preserved; provider/config mechanism deviates — see decision record). **Execution playbook (binding, authored 2026-07-04): `docs/plans/p4-execution-plan.md`** — session order O1–O9 with three review gates (F2 after O2, **F-AI live smoke after O5**, F3 after O8); build sessions start there.

- [x] **AI service module + audit plumbing + cost ceiling tables/config — DONE 2026-07-04 (O2).** `server/src/ai/` (`runAiCall` orchestrator, `gateway.js` sole SDK egress, `config.js` model/price map, `promptContext.js` throw-on-cross-facility), pure `aiBudgetService` (ceiling/cost math, 100% cov), `aiRepository`/`entitlementsRepository`, migrations `ai_call_log`/`ai_budgets`/`facility_entitlements` (all RLS). §P4 module unit tests green (mocked-gateway ceiling 79/80/100 + fire-once alert + rollover, full §9.7 field-list, scoping THROWS, no-fallback, exactly-one-retry) + `rejectMitigationOwner` guard + `aiImportBoundary` scan. **F2 Fable gate PASSED 2026-07-04** — all checklist items verified; 3 open questions resolved (operator-scope entitlement enforced by O7 job selection + `AI_SCOPE_MISSING` guard; owner-connection seam bound to O7; retry taxonomy — permanent 4xx/validation errors not retried, `502 AI_CALL_FAILED`). O3 cleared to build.
- [x] 1. Semantic library search — **DONE 2026-07-04 (O3).** `library_entries.embedding vector(1536)` (migration 202607050004); async post-commit embedding pipeline on create/update (`src/ai/libraryEmbedding.js`, fires on res "finish" so a slow/failing gateway never blocks the write; re-embeds only on title/body change); `GET /api/library/search` (embeds query → cosine `<=>` rank, facility-scoped, top 10 + similarity; 404 when AI off); `scripts/reembed-library.js` backfill (`npm run reembed`). Client: LibraryModal wired through the prod↔demo seam (`searchLibrary` in WorkspaceContext; prod hits the endpoint, demo ranks fixtures; fetch-spy test). Tests: `librarySearch.test.js` (embedding written, cross-facility isolation, deterministic ordering, type filter, audit row) + seam test. 366 unit / 152 integration / 190 client.
- [x] 2. Smart tagging — **DONE 2026-07-04 (O4).** Migration 202607050005: `tag_vocabulary` (four categories: threat_type|asset_class|region|consequence_category; unique per facility) + `scenario_tags` (evaluation-scoped; source ai|manual, status suggested|confirmed|removed), both RLS by facility. `POST /api/assessments/:id/evaluations/:evalId/suggest-tags` (Author+Draft via reused `loadWritableAssessment` gate — no lock_version bump; `rejectMitigationOwner`; 404 when AI off) → `runAiCall({kind:'object'})` with a zod `{tags:string[]}` schema (`src/ai/prompts/smartTagging.js`) → **out-of-vocab discarded** by pure `tagVocabularyService.validateTags` → persist `suggested` rows, audit `ai-tags-suggested`. `POST .../tags/confirm` re-validates + persists the Author's chosen set as `confirmed` (drops de-selected → `removed`), audit `tags-confirmed` (separate row). `GET .../tags` for hydration. Vocabulary seeded per demo facility from §19.1/§6.3/§19.5 defaults (`db/seed.js`; `seedVocabulary` reusable by O8). Client: `ScenarioTags` chips under the §6 scenario ("AI-suggested" badge, remove/add-manual/confirm, 30s auto-confirm §9.6) through the prod↔demo seam (`suggestScenarioTags`/`confirmScenarioTags`/`loadScenarioTags` in WorkspaceContext; demo no-fetch). Tests: `smartTagging.test.js` integration (2 valid + 2 invalid → exactly 2 persist; both audit sets; role/state/Mitigation-Owner/cross-tenant gating) + seam + component RTL. 379 unit / 163 integration / 198 client.
- [x] 3. Drafted Executive Summary / Conclusion — **DONE 2026-07-04 (O5).** `POST /api/assessments/:id/sections/:n/generate-draft` (n ∈ {1,8}; §9.1 gating: Author AND state != Approved → 403/409; `rejectMitigationOwner`; 404 when AI off). Gathers the Sections 2–7 bundle (`getAssessmentBundleForUser`), `buildPromptContext` scope invariant, `runAiCall({kind:'text'})` with `ai/prompts/draftedSummary.js` → 3–5 paragraphs. **AI original retained** in an `ai-draft-generated` audit row (`metadata.draftText`) — no section write, no lock bump — so it sits next to the edited final saved via the normal §P3 section path. Client: "Draft with AI" flow moved into `ExecutiveSummarySection`/`ConclusionSection` (Author + editable only), `AIDraftModal` now presentational (preview/regenerate/accept→editor) through the prod↔demo seam (`generateSectionDraft`; demo derives locally, no fetch). Tests: `draftedSummary.test.js` integration (role/state/section-number/Mitigation-Owner/cross-tenant gating; AI original next to edited final) + seam + section RTL. 386 unit / 172 integration / 202 client.
- [x] **F-AI gate (after O5, before O6) — PASSED 2026-07-16.** Live product smoke of O3–O5 against a real Vercel AI Gateway key (`AI_ENABLED=true` + `AI_GATEWAY_API_KEY`). Owner sign-off after checklist (playbook F-AI section); UX gaps from smoke fixed same day (Accept persist, Library Use entry, inline `searchLibrary`; Docker client rebuild + library embedding backfill).
- [x] 4. Anomaly detection server engine — **DONE 2026-07-16 (O6).** `POST /api/assessments/:id/anomaly-check` + `POST /api/assessments/:id/anomaly-acknowledgements` (Author + Draft via `loadWritableAssessment`; `rejectMitigationOwner`; 404 when AI off). Hybrid engine: pure `services/anomalyRulesService.js` (3 deterministic rules — asset-criticality-vs-consequences, severity-vs-criticality, R1 math) runs free, then `runAiCall({kind:'object'})` with `ai/prompts/anomalyDetection.js` for the two contextual checks. Whole feature gated on the `anomaly_detection` entitlement (checked before the free rules too) → 403 `FEATURE_NOT_ENABLED`, zero gateway calls. Advisory only: an AI failure degrades to deterministic flags (`llmAvailable:false`), never an error. Migration `202607050006_anomaly_acknowledgements` (RLS, per-Author per-assessment suppression); audits `anomaly-flagged` / `anomaly-acknowledged`. Server-only — client wiring of AD-1's chip/ack UI to these endpoints is NOT in O6 (see backlog). 408 unit / 196 integration / 205 client.
- [x] 5. Cross-facility consistency flagging — **DONE 2026-07-16 (O7).** Nightly job `server/src/jobs/consistencyFlagging.js` (`npm --prefix server run job:consistency`; Render cron invokes it after midnight UTC — setup in SESSION_LOG, no scheduler built). Per operator: entitled facilities only → cluster by (threat type, asset class) → leave-one-out peer mean/σ → flag ≥2σ outliers → prose rationale via `runAiCall({feature:'consistency_flagging', operatorId, userId:'system'})` (HQ budget scope, $20/mo §19.11). Statistics are pure in `services/consistencyService.js` (σ floored at 2 rating points — the 5×5 matrix is too coarse for raw σ; see SESSION_LOG). Migration `202607050007_consistency_flags` (RLS; upsert on (evaluation_id, cluster_key) so nightly re-runs refresh rather than duplicate and never resurrect a dismissal; unbacked pending flags → `expired`). Read surface `GET /api/assessments/consistency-flags` + `PATCH /consistency-flags/:flagId` (HQ Executive only, §17.5 portfolio scope in SQL, dismiss/send-back audited). Client: the HQ "Cross-facility AI flags" panel now renders the real rows through the seam (was a hardcoded array). 429 unit / 226 integration / 209 client.
- [ ] 6. Natural-language search — BESPOKE, build last, only when commissioned (§9.5: zero v1 hours).
- [ ] **Per-facility add-on entitlements — data model + read-time gating.** *(O2 DONE 2026-07-04: `facility_entitlements` migration + RLS, read baked into `runAiCall` — disabled add-on → 403 `FEATURE_NOT_ENABLED`, no gateway call/cost. Owner toggle + dashboard "Active capabilities" card remain in O9.)* Facility-level entitlements record; the §9 add-on features (anomaly detection, cross-facility consistency, offline mode) check it before running, so P4 never ships always-on paid features. Bake the read into the AI service module from day one (same "plumbing-in, don't retrofit" logic as the `facility_id`/trace line below). The dashboard "Active capabilities" card (`AuthorDashboard.jsx:354–379`, currently hardcoded) then renders off this instead of static props. The owner-facing toggle to flip entitlements ships in P4.5. — **DECIDED 2026-07-04:** owner-only writes for v1; feature keys `anomaly_detection` / `consistency_flagging` / `offline_mode`; base features always-on (no rows, not enabled-rows); read baked into `runAiCall` from O2. Full shape in `docs/plans/p4-execution-plan.md` (migration 3).

## P4.5 — Platform Console (owner/consultant tenant provisioning & portfolio) — SPECCED 2026-07-04

Owner-facing console for Alora (platform owner), **separate from the customer Admin role and the `/admin` surface**. Job-to-be-done: provision new clients, monitor all engagements, and support into a client context — none of which belong in the operator-scoped Admin area. Deliberately placed after P4 so tenant isolation (P2), the write/section API (P3), and the AI module (P4) are all landed first; the console provisions into a system whose trust boundaries are already proven. Precursor step is the "Staging onboarding kit" script (Suggested improvements) — ship that first, promote to UI here once onboarding volume makes hand-SQL painful.

**Scope (specced 2026-07-04; build blocks O8–O9 in `docs/plans/p4-execution-plan.md`; DoD `docs/test-specs.md` §P4.5):** a new top-privilege `PLATFORM_OWNER` role gated by BOTH a role assignment AND an env email-allowlist (`PLATFORM_OWNER_EMAILS`), MFA-required, never visible to operator users; a `/api/platform/*` router whose routes are cross-tenant **by reviewed explicit queries only** (own `middlewareCoverage` class; normal-route RLS/facility scoping provably unweakened); one-transaction provisioning (operator → facility → initial Admin → role assignments → §19 seed defaults, optional copy-libraries-from-operator) with a platform audit row per provision; a read-only owner overview (operators/facilities counts, assessments by state, handover status §5.7, stale engagements, month-to-date AI spend from `ai_call_log`); the owner-side entitlement toggle over the P4 model; and **link-only support access** (below). Console UI is deliberately minimal — an internal tool for one user.

- [ ] **New `PLATFORM_OWNER` role** — highest privilege; NOT "Admin + flag". Env-allowlisted owner emails (small fixed list), MFA required, stricter session policy. Never visible to operator users. DoD: `docs/test-specs.md` §P4.5 — role gating matrix (operator roles → 403 on all `/platform/*`; owner → 200), allowlist enforcement, and a cross-tenant test proving the console does NOT weaken RLS/facility isolation for normal routes.
- [ ] **Provisioning API + wizard** — one-transaction create: operator → facility(ies) → initial Admin user + role assignments, seeded from §19 defaults (threat classifications, 5×5 matrix, risk bands, libraries) with optional "copy from existing operator". Writes a platform audit entry per provision. Route namespace `/platform/*`, reviewed explicit queries (no generic RLS bypass).
- [ ] **Owner dashboard** — all clients in one view: operators/facilities counts, assessments by state across tenants, handover status (§5.7 consultant-led vs operator-owned), stale/blocked engagements, optional per-operator AI budget usage (§9). Read-only aggregate; must respect operator boundaries in its own queries.
- [ ] **Support access — DECIDED 2026-07-04: link-only.** Owner self-assigns a normal role in the target operator/facility via an audited `/platform` grant (`platform-support-access-granted`/`-revoked`), then uses the existing multi-role switch + MFA-at-role-switch machinery (§3.2/§4.3) and appears in tenant audit under their real user id. Matches §17.6's "role flag, not special user type"; NO impersonation/dual-identity session in v1 (recorded as possible future hardening).
- [ ] **Add-on entitlement toggle (owner).** Write surface + audit row over the P4 entitlements model — the owner flips an add-on On/Off per facility after a customer "enquires to enable" (paid recurring add-ons; owner-only for v1). Customer-Admin self-serve upgrade is a later billing decision, not v1.

## P5 — Hardening

DoD: `docs/test-specs.md` §P5 — full reset-email loop with mocked transport, retention-job seed/delete/survive/audited test, Redis swap passing existing tests unchanged, PII-scrub assertion on tracked errors.

- [ ] Wire stubbed email delivery (password reset, notifications). (Word/PDF export moved to P3.5, 2026-07-03.)
- [ ] Monitoring / error tracking (Render logs + an error tracker; recommend Sentry).
- [ ] Audit retention policy (default 7 years, per-facility configurable — §18.3).
- [ ] Redis for rate-limit + MFA replay caches (required before multi-instance scaling).
- [ ] Backups: verify Supabase daily backups / PITR meet §18.5; document restore drill.
- [ ] TLS/CA pinning for DB connection (replace `rejectUnauthorized: false`).

## P6 — Offline / Field mode — SPECCED 2026-07-04 (build after P4.5)

Paid per-facility add-on (`offline_mode` entitlement, key already in the P4 model); free tier gets §8.4 read-only fallback + PWA shell. **Architecture decided 2026-07-04** (`docs/decisions/2026-07-04-offline-mode-architecture.md`): whole-assessment exclusive checkout (supersedes businesslogic §8 per-section — sections are workflow stages, not specialist territories; future granularity is domain-scoped, customer-driven), sync replays through `contentWriteGuard` with zero conflicts by construction, PIN-only offline auth encrypting the IndexedDB cache, deviceEditAt in audit metadata. Photos out of v1 (no attachments feature exists online). **Binding playbook: `docs/plans/p6-offline-execution-plan.md`** (O1–O6, Fable gates F4 after O3 / F5 after O6; feature stays dark until F5 passes). DoD: `docs/test-specs.md` §P6. Hard dependency: P4's `facility_entitlements` migration — O2+ must not start before it is on `main`; O1 has no dependency and can run any time.

- [ ] O1 — Graceful degradation (free tier): connectivity detection + banner, §8.4 read-only fallback, PWA shell (`vite-plugin-pwa`, precache app shell only).
- [ ] O2 — Checkout model (server): `offline_checkouts` migration + RLS, checkout/release/force-release endpoints, `contentWriteGuard` step 3.5 (+ workflow/lead-author block), "checked out by" read surface, entitlement gating.
- [ ] O3 — Sync replay engine (server): `offline_sync_batches`, op dispatch → existing mutators, all-or-nothing batches, idempotent retries, offline audit metadata, cross-tenant sync isolation suite. **→ STOP for F4 Fable gate.**
- [ ] O4 — Client offline store: IndexedDB (`idb`), op queue, WorkspaceContext third persistence branch, checkout UI replacing `FieldModePage`/`FieldModeModal`, snapshot hydration.
- [ ] O5 — Sync UX end-to-end: queue screen, reconnect sync, revoked/failed recovery incl. export-queue-to-file.
- [ ] O6 — PIN pre-authorise: PBKDF2/AES-GCM cache encryption, attempt counter + wipe + audited wipe-report, signed device token. **→ STOP for F5 Fable gate; `offline_mode` stays disabled everywhere until F5 passes.**

## Suggested improvements & new features

Living review of every major feature area: what exists / what's partial / what's missing / what could be better, plus new-feature suggestions. **Priority here is suggestion-priority (P0 = do soon / blocks value, P1 = high value, P2 = later), NOT the phase numbers above. Effort: S/M/L.** Each suggestion carries a status: **SCHEDULED** (already inside a phase above — no new approval needed), **BACKLOG** (agreed direction, slot it when capacity allows), or **NEEDS DECISION** (user must approve scope/phase before any work). Nothing gets implemented outside an approved phase without user approval. Status claims marked *(verify)* were taken from docs/session-log, not re-read from code this session.

### Area-by-area review (2026-07-03)

| Area | Exists | Partial / missing | Improvement suggestions |
|---|---|---|---|
| **Auth** | Login/JWT/refresh/rotation, logout+revocation, password reset flow, TOTP MFA + lockout + trusted devices, demo gating | Reset **emails are stubbed** (link never delivered); account-lockout on password (not just MFA) *(verify)*; session-timeout not per-facility configurable (§18.1) | [P0/S] wire email — **SCHEDULED (P5)** · [P1/S] admin "reset user's MFA/password" action — **BACKLOG** · [P2/S] configurable session timeouts — **BACKLOG** |
| **Assessment workflow** | Server state machine + transitions + audit + version snapshot on Approve; allowed-actions API | Withdraw/recall (§5.6) server support *(verify)*; recall race = known concern (AGENTS.md); Lead Author reassignment (§5.5) unbuilt server-side | [P1/M] withdraw/recall + reassignment endpoints alongside P3 writes — **SCHEDULED (P3, approved 2026-07-03)** |
| **Sections 1–9 editing** | Rich demo editors client-side; validation w/ human labels; AD-1 anomaly ack on §3 | **Zero server persistence (the P3 gap)**; no DB column for Section 1/2/8 text; comments/field-level review locks (§11.2) demo-only *(verify)* | [P0/L] write API — **SCHEDULED (P3)** · [P1/M] server-side comments + review locks after P3 — **BACKLOG** · [P2/M] autosave + draft-recovery UX — **BACKLOG** |
| **Mitigations** | `GET /mine` + `POST /:id/log` real; owner workflow client-built; progress logs table | Assignment management (who owns what) has no write API; pool role-holder change behaviour (§7.4) unbuilt | [P1/S] assignment endpoints — **SCHEDULED (P3, approved 2026-07-03)** · [P2/S] AI progress-log summarisation (§9.8, explicitly post-v1) — **BACKLOG** |
| **Admin** | `GET /api/admin/configuration` only; admin UI on fixtures | No server CRUD for users, facilities, role assignments, dropdowns, MFA policy, libraries — all §13 surfaces | [P1/L] **Admin write API** phase (new — number TBD; *not* "P3.5", which is the completed export phase) — **NEEDS DECISION** · [P0/S] interim documented SQL snippets for staging onboarding — **BACKLOG** (do with P0) |
| **Audit log** | Append-only writes w/ hash-chain fields on workflow + auth events | **No read endpoint at all** (client /audit is fixtures); per-role visibility (§10.2) undecided (AGENTS.md open concern); retention = P5 | [P1/M] `GET /api/audit` w/ role/facility scoping — **NEEDS DECISION** (§10.2 visibility question is an AGENTS.md flagged concern) · [P2/S] hash-chain verification job — **BACKLOG** |
| **Field mode / offline** | Client messaging + online-only feature list (illustrative) | Whole §8 unbuilt (stubs only) | **SCHEDULED (P6)** — specced 2026-07-04 (decision resolved: whole-assessment checkout + PIN-only, supersedes §8 per-section; `docs/decisions/2026-07-04-offline-mode-architecture.md`); build after P4.5 per `docs/plans/p6-offline-execution-plan.md` |
| **Exports (Word/PDF)** | **P3.5 COMPLETE (2026-07-04)** — server `GET /export?format=docx\|pdf` (docx lib + pdfkit, front-matter + frozen snapshot + audit + role matrix, 15 integration tests) **and** client Export button (workspace Tools → `ExportModal`, prod blob download / demo no-fetch, RTL suite) | — | Custom corporate export templates = Phase 3 add-on (§16.5) — **BACKLOG** |
| **Notifications** | Client inbox model, role-filtered, fixtures; triggers specced (§15) | No server model/endpoints/email channel | [P2/M] notifications table + triggers built WITH P3 mutations (write points are where triggers live; cheap then, expensive later) — **NEEDS DECISION** (adds to P3 scope) |
| **AI (6 features)** | AD-1 ack loop client-side; §9 module specced; gateway decision recorded | Everything server-side = P4 | **SCHEDULED (P4)** · [P1/S] stamp `facility_id` + trace plumbing through all P3 routes for clean P4 drop-in — **BACKLOG** (fold into P3 execution) |
| **HQ Executive view** | Dashboard on fixtures; heatmap/KPIs specced (§14) | No aggregate server queries; consistency flagging = P4 | [P1/M] read-only portfolio aggregates endpoint after P2 — **BACKLOG** (high demo value; isolation must land first) |
| **Mobile UX** | Mobile-first shells, demo mobile gate, whole-row tap target on Author dashboard | Same tap-target fix pending on Reviewer/Approver/HQ/MitOwner dashboards; §5 grid virtualization | [P1/S] replicate row tap-target fix (pattern proven) — **BACKLOG** · [P2/M] virtualization at real data scale — **BACKLOG** |
| **Dark mode** | ~52%; auth pages tokenized; login brand treatment | Toggle on auth routes; `prefers-color-scheme`; shared components; dashboards | **SCHEDULED (side-quest)** — [P1/S] chunks, any time |

### Security audit follow-ups (AUDIT-BRIEF pass, 2026-07-10)

Landed on branch `security/audit-hardening-2026-07-10` (awaiting owner review): auth rate limiting + `trust proxy` (H1/M5), helmet (M6), acting-role from token claim not header (M4), recovery-code lockout (M7), RLS-role boot guard (M2), admin-reset ADMIN gate (L1), non-local secret guard (L2), JWT alg pin (L5), mitigation-log validation (L7), server `npm audit fix` + client react-router 6.30.4. **Confirmed already-deferred, left for P5:** DB TLS CA-pinning + Redis rate-limit/replay caches (single-instance deploy). Remaining un-fixed gaps:

- [P1/M] **Access token moved out of localStorage** (M3) — hold the access JWT in memory, rehydrate via the httpOnly `/refresh` cookie on load; XSS-exfiltration hardening. Client rearchitecture + minor flow change; no live XSS sink today. — **BACKLOG**
- [P2/S] **Single-use-token check-then-act races** (L6) — recovery-code / password-reset / promotePending ignore the update row count; assert count==1 or `FOR UPDATE`. Low risk on single-instance. — **BACKLOG**
- [P2/S] **Mitigation-owner assignment role check** (L8) — verify the assignee holds Mitigation Owner at the facility (mirror the lead-author `userHasFacilityRole` guard); data-integrity, not IDOR. Needs an integration fixture update. — **BACKLOG**
- [P2/S] **AI prompt-delimiter neutralisation + facility-id assertion** (L9/L11) — strip `"""` from interpolated user text; throw (not skip) when a prompt entity lacks `facility_id`. Contained today by the scope invariant. — **BACKLOG**
- [P2/S] **appendAuditLog GUC restore** (L10) — nested `SET LOCAL` narrows the request txn's facility scope; latent false-deny for a future multi-facility-in-one-txn handler. — **BACKLOG**

### New feature suggestions (not in any current phase)

- [P1/M] **Annual clone / carry-forward** — already specced (§6.10) but in no phase; natural P3 follow-on reusing the write API; the yearly-SRA cadence is the core usage loop, so this is the retention feature. — **NEEDS DECISION** (which phase)
- [P1/S] **Staging onboarding kit** — scripted creation of a real operator/facility/users (replaces hand-SQL once P0 lands; precursor to the admin write API). — **BACKLOG**
- [P2/M] **Version history UI + diff** (§10.4–10.5) — snapshots already written on Approve; surfacing them is mostly read API + UI. — **BACKLOG**
- [x] **Library management CRUD + seeding** (§12) — DONE 2026-07-04 (O1). `/api/library` module (CRUD on `library_entries`, five types; reads any facility role, writes Admin-only; `facilityScope`+`requireFacilityAccess`), hyphen-vocabulary audit rows (`library-entry-created|updated|deleted`), §19-default seed content per facility (idempotent). Integration CRUD + role matrix + cross-tenant + audit suite (`library.test.js`). Prereq for P4 feature 1 (semantic search) — table/endpoints now exist to search.
- [P2/S] **E2E smoke harness (Playwright) against staging** — once P0 exists, one scripted login→edit→submit→approve run guards every deploy; cheap insurance before P3's client flip. — **BACKLOG**
- [P2/S] **Status/uptime probe** on `/health` + Supabase (free tier UptimeRobot) — staging will be demoed; know it's down before the prospect does. — **BACKLOG** (fold into P5 monitoring)
- [P2/L] **Offline v2: domain-scoped checkout** — row-level scope across §4/§5/§6 for parallel field specialists (supersedes §8's per-section idea; schema already carries `scope` jsonb). Design only against real customer demand. — **BACKLOG**
- [P2/M] **Offline v2: biometric unlock (WebAuthn) + tamper-resistant attempt counter** — hardens the P6 PIN model per §8.3's full spec. — **BACKLOG**
- [P2/M] **Offline photo/attachment queue** — blocked on attachments existing online at all (no table/endpoints today); revisit when an attachments feature ships. — **BACKLOG**
- [x] **Prod dashboards empty (P3 (g) follow-on) — DONE 2026-07-04.** Dashboards rendered from the fixture-seeded in-memory store, so in prod they filtered to 0 (demo ids ≠ real session). Added `WorkspaceContext.hydrateAssessmentsList` (prod fetch `GET /api/assessments` → replace `assessmentsById`), triggered once in `AppShell`; `filterAssessmentsForRole({ serverScoped })` skips per-user narrowing in prod (Author/Reviewer/Approver dashboards). +4 client tests. NB: dashboard child-entity stat cards (assets/evals) still read fixtures in prod — separate follow-on.
- [x] **Evaluation create-on-link (server gap from P3 (g)) — DONE 2026-07-04.** Chose auto-create over a separate POST: `setAssetThreatLink` seeds an empty evaluation when a pair is enabled and echoes it in the `PUT /links` response; the client adds the returned UUID row so Section 6 PATCHes work for freshly-ticked cells. Disable never deletes the evaluation (re-enable restores work). +2 integration tests. `persistEvaluation`'s UUID guard still protects any pre-hydration stub.
- [P1/S] **HQ consistency triage UI (O7 follow-on).** `PATCH /api/assessments/consistency-flags/:flagId` (dismiss with reason / send back to Author) ships and is integration-tested, but no client surface calls it — the HQ panel is read-only, so §9.3's "review, dismiss, or send back" loop is half-built. Needs a reason picker (dismiss requires a reason server-side) + optimistic row removal through the seam. — **BACKLOG**
- [P2/S] **Align seeded threat `details.classification` to the §19.1 vocabulary.** `db/seed.js` writes only `"Cyber"`/`"Physical"` where client fixtures and §19.1 use the default-8 names ("Cybercrime & Data Breaches", "Maritime", …). O7 clusters on `classification` (falling back to `name`, DECIDED 2026-07-16), so seeded data collapses into two coarse clusters and under-reports outliers on staging. Real facility data is unaffected. — **BACKLOG**
- [P1/M] **AD-2 client wiring — hook AD-1's chip/ack UI to the O6 server engine.** O6 shipped server-only, so `useAnomalyAcknowledgement` still runs the rule locally (`client/src/data/assets.js detectAssetAnomaly`) and stores acks in the asset `details` bag. Wiring needs: `POST /anomaly-check` behind the existing 800ms debounce through the prod↔demo seam, chips for the evaluation-scoped rules (§5/§6, AD-1 only renders §3), and a mapper from the picker's display strings (`ACK_REASONS`) to the API's storage vocabulary (`not_applicable|false_positive|will_address|other`). Demo keeps the local rule. — **BACKLOG** (needs a slot; entitlement toggle O9 makes it demoable)
- [P2/S] **Anomaly rule: residual R2 exceeding inherent R1.** Post-mitigation risk scoring higher than pre-mitigation is arithmetic that cannot be right, and it is cheap to detect in `anomalyRulesService`. Deliberately NOT built in O6 — §9.2 names three deterministic rules and the plan binds them; this is a fourth. — **BACKLOG** (needs approval as a rule-catalogue addition)
- [P2/S] **Anomaly audit volume tuning.** `anomaly-flagged` writes one hash-chained row per check that raises flags; with the 800ms debounce a long editing session can write many near-identical rows. If the tuning signal gets noisy, only audit when the raised set CHANGES for that Author. — **BACKLOG** (watch after the AD-2 client wiring makes real traffic)
- [x] **P4 F-AI smoke UX gaps — DONE 2026-07-16.** Accept draft on §1/§8 now persists + toasts (no blur required); Library modal Use entry applies to the focused §6 scenario via `applyLibraryEntry`; inline library matches use `searchLibrary` (real embeddings in prod).

## Side-quest — Dark mode (~52%, cosmetic, any time)

- [ ] Brand/logo treatment on remaining 6 auth pages; gold CTA app-wide decision.
- [ ] Theme toggle on login/MFA routes; honor `prefers-color-scheme`.
- [ ] Shared components (Chunk B) + dashboards/workspace/admin.
- [ ] Critical-severity dark text — awaiting designer sign-off (non-code blocker).
